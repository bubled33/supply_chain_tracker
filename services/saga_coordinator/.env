# --- Service Configuration ---
SERVICE_NAME=saga-coordinator
ENVIRONMENT=local
LOG_LEVEL=INFO

# --- Database (PostgreSQL) ---
# Replace 'user', 'password', and 'db_name' with your actual local credentials
DATABASE_URL=postgresql://***REMOVED***@localhost:5432/supply_chain_tracker
DB_POOL_MIN_SIZE=5
DB_POOL_MAX_SIZE=20

# --- Kafka (Messaging) ---
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
KAFKA_GROUP_ID=saga-coordinator-group-v1

# --- Business Logic / Saga Filters ---
SAGA_STEP_TIMEOUT_SECONDS=3600

